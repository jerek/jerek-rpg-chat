<div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
        <div class="container">

            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </a>

            <a class="brand" href="{{ url('home') }}">{{ config().title }}</a>

            <div class="nav-collapse collapse">
                {{ navigationMenu().renderPartial(zfcUserIdentity() ? 'default-user' : 'default', 'navigation/menu')|raw }}
            </div>

            <div class="nav-collapse collapse pull-right">
                {{ navigationMenu().renderPartial(zfcUserIdentity() ? 'user' : 'login', 'navigation/menu')|raw }}
            </div>

        </div>
    </div>
</div>
